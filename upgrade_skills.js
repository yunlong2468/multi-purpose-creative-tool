// 阶段二：Skill 优化 — 升级写作技能库
// 运行：node upgrade_skills.js

const initSqlJs = require('sql.js');
const fs = require('fs');

initSqlJs().then(SQL => {
    const db = new SQL.Database(fs.readFileSync('data.db'));

    function updateSkill(id, name_cn, content, description) {
        db.run('UPDATE optimized_skills SET content=?, description=?, updated_at=CURRENT_TIMESTAMP WHERE id=?',
            [content, description, id]);
        console.log('  ✅ 已更新 [' + id + '] ' + name_cn);
    }

    function insertSkill(name_cn, name_en, content, description) {
        // 获取第一个用户ID
        const r = db.exec('SELECT id FROM users LIMIT 1');
        const userId = (r.length && r[0].values.length) ? r[0].values[0][0] : 1;
        db.run(`INSERT INTO optimized_skills (user_id, name_cn, name_en, description, content, source, is_enabled, created_at, updated_at)
                VALUES (?,?,?,?,?,?,?,CURRENT_TIMESTAMP,CURRENT_TIMESTAMP)`,
            [userId, name_cn, name_en, description, content, 'system_upgrade', 1]);
        console.log('  ✅ 已新增 ' + name_cn);
    }

    console.log('=== 2.2 世界观设计指南 — 加结构化输出格式 ===');
    updateSkill(7, '世界观设计指南', `# 世界观设计指南

## 场景
当用户提出"世界观设计指南"相关操作或要求设计小说世界观时，调配师应按照本指南执行，帮助用户构建逻辑自洽、细节丰富的幻想或现实世界观。

## 操作步骤
1. **需求理解**：与用户沟通，明确世界观的核心要素，包括题材（如奇幻、科幻、都市、历史）、世界范围（单一大陆、多星球、多层位面）、核心冲突背景、魔法/科技水平等。
2. **加载技能**：若本指南尚未加载，调用 \`load_skill\` 工具加载"世界观设计指南"。
3. **生成世界观框架**：调用 \`design_worldview\` 工具，传入以下参数（根据用户需求映射）：
   - \`genre\`：题材类型
   - \`core_theme\`：核心主题（如生存、秩序与混乱、力量代价）
   - \`world_scale\`：世界规模（如星球、大陆、城市、虚拟空间）
   - \`details\`：用户补充的具体要求（如"要有浮空岛屿"、"基于五行元素"）
4. **提取世界观结构化数据**：世界观生成完成后，调用 \`extract_worldview_structure\` 工具，将世界观文本转化为结构化实体和关系。LLM应输出如下JSON格式：

\`\`\`json
{
  "实体": [
    { "临时ID":"e1", "名称":"沧玄界", "类型":"世界", "描述":"...", "父临时ID":null },
    { "临时ID":"e2", "名称":"青云宗", "类型":"势力", "描述":"...", "父临时ID":"e1" }
  ],
  "关系": [
    { "源临时ID":"e2", "目标临时ID":"e3", "关系类型":"敌对", "描述":"..." }
  ]
}
\`\`\`

**实体类型枚举**：世界、势力、地点、力量等级、物种、人物、物品、概念、事件
**关系类型枚举**：敌对、同盟、从属、师徒、亲属、竞争、中立、克制
**规则**：父临时ID为null表示根节点；势力/地点/力量体系按parent_id构建树形层级

5. **关联角色与剧情**：如果需要，可将生成的世界观文档作为上下文，调用 \`generate_characters\` 设计符合该世界观的代表性角色，或调用 \`generate_outline\` 规划围绕世界观冲突的剧情大纲。
6. **反馈与迭代**：将生成结果展示给用户，根据用户反馈进一步调用 \`design_worldview\` 调整参数，细化特定部分。

## 注意事项
- 世界观设计应服务于故事主题，避免过度设计却与剧情脱节。
- 若涉及现实历史背景，需提示用户注意考据；若为完全架空，需确保内部规则自洽。
- \`design_worldview\` 生成的内容可能较长，可建议用户分步确认，先产出总览再细化子系统（如魔法规则、种族分布）。
- 每次修改世界观后，应检查已有角色和大纲是否需要同步调整。
- 结构化提取的实体和关系将用于后续的"世界观层级树"可视化。`, '系统默认技能（已升级：加实体/关系结构化提取格式）');

    console.log('=== 2.3 角色设计指南 — 加主角标记+关系网要求 ===');
    updateSkill(4, '角色设计指南', `# 角色设计指南

## 适用场景
当用户要求设计小说角色时。

## 操作步骤
1. **确认角色需求**：与用户确认需要设计角色的数量、类型（主角、反派、配角等），以及故事的世界观背景。
2. **调用生成工具**：调用 \`generate_characters\` 工具生成角色档案。
3. **主角确认**：角色生成后，智能体应主动识别可能的主角角色，向用户提议并等待确认。一旦确认，将该角色的 is_protagonist 标记为 1。主角是整个故事的核心锚点，将用于后续的角色星座图可视化。
4. **关系网完善**：确保每个角色档案中包含"关系网"字段，描述该角色与其他角色的关系。关系应具体到"敌对/同盟/从属/师徒/亲属"等类型。如果生成的角色的关系网不够完整，调用 \`update_character_relations\` 工具补充。
5. **汇报结果**：生成完成后向用户汇报结果，并特别标注已被确认为主角的角色。

## 角色档案结构化要求
每个角色应包含以下字段：
- 姓名、别名、性别、年龄（外表/实际）
- 外貌描写（100-200字，具体可画）
- 性格特征、背景身世
- 能力/功法/特长
- 命运弧线（从开篇到结局的完整变化）
- 对白风格（说话方式、口头禅）
- **关系网**：列出与其他角色的关系，格式为 {角色名: 关系类型, ...}
- **主角标记**：is_protagonist=1 表示主角，默认为 0

## 重要
- 必须调用 generate_characters 工具，严禁自己编写角色档案
- 如果用户对角色不满意，可以再次调用工具重新生成
- 主角确认是必要环节，不可跳过`, '系统默认技能（已升级：加主角标记提示+关系网要求）');

    console.log('=== 2.4 大纲设计指南 — 加时间线事件提取格式 ===');
    updateSkill(5, '大纲设计指南', `# 大纲设计指南

## 适用场景
当用户确认需求后需要生成小说大纲时。

## 操作步骤
1. 确认已完成需求采访和摘要确认
2. 调用 \`generate_outline\` 工具生成分卷分章大纲
3. 大纲生成后，从各章节中提取关键事件，填入时间线。时间线事件格式如下：

\`\`\`json
{
  "事件": [
    {
      "事件名": "主角获得金手指",
      "概要": "简述事件内容",
      "涉及角色": ["主角", "师父"],
      "绝对年份": 0,
      "纪元名称": "灵气复苏元年",
      "势力历法": {"青云宗":"开宗第300年", "魔教":"黑暗纪元第7年"},
      "事件类型": "major",
      "关联伏笔": "山洞奇遇"
    }
  ]
}
\`\`\`

**字段说明**：
- \`绝对年份\`：REAL数值，以故事主线起点为0，支持负值表示前传事件
- \`纪元名称\`：该事件所处的时代/纪元名称
- \`势力历法\`：JSON对象，不同势力对同一时间的纪年方式
- \`事件类型\`：major(核心转折)/minor(日常过渡)/foreshadow(伏笔埋设)/payoff(伏笔回收)
- \`关联伏笔\`：如果此事件涉及埋设或回收伏笔，在此标注伏笔名称

4. **伏笔标记**：在生成大纲时，智能体应识别可埋设伏笔的节点（如神秘人物登场、异常现象、未解之谜），并在对应章节的事件中标注事件类型为 foreshadow。后续在伏笔管理指南中统一规划回收时机。
5. 生成完成后向用户汇报卷章数量和关键时间节点。

## 重要
- 必须调用 generate_outline 工具，严禁自己编写大纲
- 用户可以要求调整大纲结构
- 时间线事件按绝对年份排序（非章节顺序），用于时间线视图可视化`, '系统默认技能（已升级：加时间线事件提取格式+伏笔标记）');

    console.log('=== 2.5 通用写作技能库补充 ===');

    // 2.5a 伏笔管理指南
    insertSkill('伏笔管理指南', 'foreshadowing_management', `# 伏笔管理指南

## 适用场景
当用户需要管理故事中的伏笔（埋设/跟踪/回收）时使用本指南。

## 操作步骤
1. **列出当前伏笔**：查看已有的伏笔列表（foreshadowing表），展示其状态（已埋设/已回收/待确认）。
2. **埋设新伏笔**：当用户或智能体在大纲/对话中提出可埋设的伏笔时，调用 \`create_foreshadowing\` 工具：
   - \`name\`：伏笔名称（简洁标识）
   - \`description\`：伏笔描述（埋设了什么线索，暗示了什么）
   - \`plant_chapter_id\`：埋设章节ID
   - \`status\`：初始为 "planted"
3. **跟踪伏笔状态**：在大纲推进或章节写作时，检查哪些伏笔已到回收时机，提醒用户。
4. **回收伏笔**：当伏笔被揭示时，调用 \`resolve_foreshadowing\` 工具：
   - 设置 \`resolve_chapter_id\`（回收章节）
   - 更新 \`status\` 为 "resolved"
   - 记录 \`resolved_at\` 时间戳
5. **伏笔与时间线联动**：伏笔埋设和回收事件应在时间线（plot_timeline_events）中对应标记：
   - 埋设点 → \`event_type = "foreshadow"\`
   - 回收点 → \`event_type = "payoff"\`
   - 两个事件通过 \`关联伏笔\` 字段指向同一伏笔名称

## 伏笔类型
- **道具伏笔**：某件物品在后期发挥关键作用
- **人物伏笔**：某角色的真实身份/能力隐藏
- **对话伏笔**：某句看似随意的话暗藏玄机
- **场景伏笔**：某个地点/场景的特殊意义
- **规则伏笔**：世界观规则的隐藏层面

## 注意事项
- 伏笔不宜过多（长篇建议 15-30 个），过多会导致读者遗忘
- 重要伏笔至少埋设一次、暗示一次，再回收
- 伏笔回收间隔不宜过短（至少间隔 5 章以上）
- 定期检查未回收伏笔，防止"挖坑不填"`, '系统升级：伏笔管理技能');

    // 2.5b 冲突设计指南
    insertSkill('冲突设计指南', 'conflict_design', `# 冲突设计指南

## 适用场景
当用户需要设计故事冲突（角色间冲突/内部冲突/世界观冲突）时使用本指南。

## 冲突层次模型
故事冲突分为四个层次，智能体应逐层分析和设计：

### 第一层：内部冲突（角色内心）
- 核心价值观冲突（如忠诚vs自由、复仇vs宽恕）
- 身份认同冲突（如人vs魔、凡人vs英雄）
- 道德困境（两难选择，无论选哪边都有代价）

### 第二层：人际关系冲突
- 主角vs对手（核心对抗关系）
- 主角vs盟友（目标分歧、方法争议）
- 三角冲突（A-B-C三方利益纠缠）

### 第三层：社会/势力冲突
- 宗门对立（正邪、新旧、强弱）
- 阶层矛盾（贵族vs平民、修炼者vs凡人）
- 资源争夺（灵脉、功法、领土）

### 第四层：世界观冲突
- 规则对抗（如灵力vs科技、天劫vs长生）
- 天命vs自由意志
- 世界存亡级威胁

## 操作步骤
1. **分析现有冲突**：读取角色关系（relationship_edges）和世界观设定，识别已有冲突。
2. **设计冲突弧线**：为每个核心冲突规划"建立→升级→转折→解决"四阶段弧线，并在时间线中标记关键事件。
3. **冲突交织**：将不同层次的冲突交织在一起（如内部冲突在人际关系冲突中暴露，人际关系冲突反映社会冲突）。
4. **调用工具**：使用 \`update_character_relations\` 更新角色间冲突关系，使用 \`update_timeline\` 标记冲突升级节点。

## 冲突验证清单
- [ ] 主角有明确的内部冲突吗？
- [ ] 主角和对手的冲突有"不可调和"的核心原因吗？
- [ ] 冲突升级是否有递进感（不是一跳就到顶点）？
- [ ] 次要角色之间是否有独立于主角的冲突？
- [ ] 冲突的解决是否与角色成长相关联？

## 注意事项
- 避免"纸片人"反派——好的对手有自己的正当理由
- 冲突不应只有外部打斗，心理博弈和策略对抗同样重要
- 每个冲突应有"代价"——赢也有损失，输也有收获`, '系统升级：冲突设计技能');

    // 保存
    const data = db.export();
    const buffer = Buffer.from(data);
    fs.writeFileSync('data.db', buffer);
    console.log('\n✅ 所有Skill升级完成，数据库已保存');
    db.close();
});
